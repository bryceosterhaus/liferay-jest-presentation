test('return emojis with hands', () => {
  const val = emojiSearch('hands');

  expect(val).toEqual(
    [
      {descriptors: 'praise hands', emoji: '🙌'},
      {descriptors: 'clapping hands', emoji: '👏'}
    ]
  );
});
